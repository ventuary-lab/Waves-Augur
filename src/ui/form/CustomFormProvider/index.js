import React from 'react';
import { html } from 'components';

const bem = html.bem('CustomFormProvider');

class CustomFormProvider extends React.Component {
    constructor (props) {
        super(props);
    }

    render () {
        <div></div>
    }
}

export default CustomFormProvider;